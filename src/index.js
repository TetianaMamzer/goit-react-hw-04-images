import React from 'react';
import ReactDOM from 'react-dom/client';
import  SearchImg  from 'components/App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SearchImg />
  </React.StrictMode>
);
